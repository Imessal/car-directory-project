CREATE TABLE `carDb`.`car` (
                                      `number` VARCHAR(10) PRIMARY KEY,
                                      `brand` TEXT NOT NULL,
                                      `model` TEXT NOT NULL,
                                      `color` TEXT NOT NULL,
                                      `year` INTEGER NOT NULL
)